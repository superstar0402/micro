{
  "name": "noir-starter",
  "version": "1.0.0",
  "description": "A template repository to get started with writing zero knowledge programs with Noir.",
  "scripts": {
    "dev": "hardhat compile && yarn deploy && next",
    "build": "hardhat compile && yarn deploy && next build",
    "build1": "next build",
    "start": "next start",
    "deploy": "hardhat compile && hardhat run --network ${NETWORK} scripts/deploy.ts",
    "test": "hardhat test"
  },
  "type": "module",
  "dependencies": {
    "@iexec/dataprotector": "^0.3.0",
    "@noir-lang/backend_barretenberg": "^0.17.0",
    "@noir-lang/noir_js": "^0.17.0",
    "@noir-lang/noir_wasm": "^0.17.0",
    "@noir-lang/source-resolver": "^0.17.0",
    "@noir-lang/types": "^0.17.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "@openzeppelin/contracts": "^4.0.0",
    "@walletconnect/modal": "^2.6.2",
    "@web3modal/ethers5": "^3.2.0",
    "dotenv": "^16.0.3",
    "ethers": "5.7.2",
    "hardhat": "^2.17.0",
    "next": "latest",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-toastify": "^9.1.1"
  },
  "devDependencies": {
    "@types/react": "18.2.34",
    "chai": "^4.2.0"
  }
}
